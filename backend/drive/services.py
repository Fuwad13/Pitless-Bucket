from sqlmodel.ext.asyncio.session import AsyncSession
import uuid


class DriveService:
    async def get_files_list_by_user(self, session: AsyncSession, user_uid: str):
        pass

    async def get_files_list_by_user(
        self,
        session: AsyncSession,
    ):
        pass
